# Changelog

## 2025-11-21

### Добавлено
- ✅ **Артефакты в YAML** - каждая находка сохраняется как отдельный файл для просмотра
- ✅ **Команда `artifacts`** - просмотр, фильтрация и управление находками
- ✅ **Структурированное хранилище** - артефакты организованы по источникам
- ✅ **Конфигурация в YAML** - все настройки и промпты в `config.yaml`
- ✅ **Промпты в конфиге** - интересы как часть промпта, не отдельный файл

### Изменено
- 🔄 **Убрана конкурентность** - последовательная обработка для отладки
- 🔄 **Убран interests.md** - интересы встроены в промпт в config.yaml
- 🔄 **Убран pydantic-settings** - простые dataclass + YAML
- ✅ Парсинг HuggingFace Papers через извлечение JSON из React hydration
- ✅ Keyword-фильтрация статей по темам speech/audio/music (30+ ключевых слов)
- ✅ Фильтрация HF моделей по дате обновления (только за последние 2 недели, сортировка по trending score)
- ✅ Эмодзи для источников в логах (🐙 GitHub, 📄 Papers, 🤖 Models)
- ✅ Улучшенная структура логов с явными этапами (СБОР → ДЕДУПЛИКАЦИЯ → ФИЛЬТРАЦИЯ → АГРЕГАЦИЯ → ГЕНЕРАЦИЯ)
- ✅ Debug режим (`--debug`) для сохранения собранных данных и результатов фильтрации
- ✅ Команда `history` для просмотра статистики и очистки старых записей
- ✅ Retry логика с exponential backoff для Claude API (обработка 429 rate limits)
- ✅ Батчевание запросов к LLM (2 параллельно по умолчанию)
- ✅ Rate limiting между запросами (1.5s задержка)

### Изменено
- Логи теперь показывают:
  - Какой источник парсится (с эмодзи)
  - Сколько найдено элементов
  - Сколько отфильтровано по ключевым словам/датам
  - Прогресс фильтрации по батчам
  - Релевантность каждого элемента с причинами
- HFPapersSource теперь фильтрует статьи по 30+ ключевым словам
- HFTrendingSource использует дефолтную сортировку (по `trendingScore`) с клиентской фильтрацией по дате
- Добавлен запрос деталей модели для получения `lastModified` (API без sort не возвращает эту информацию)
- Более консервативные дефолты для избежания rate limits (2 параллельных запроса, 1.5s задержка)

### Исправлено
- HuggingFace Papers не парсились (теперь работает через JSON extraction)
- HuggingFace API возвращал 400 из-за неправильного параметра sort
- Ложные срабатывания фильтрации (убраны слишком общие слова типа "synthesis", "ASR")
- Rate limit 429 от Claude API (добавлены retries, batching, увеличены задержки)

## Файлы и директории

- `artifacts/` - YAML артефакты всех находок (по источникам)
- `interests.md` - правила отбора
- `digests/` - сгенерированные дайджесты
- `debug/` - отладочные данные (при `--debug`)

## Пример YAML артефакта

```yaml
title: maya-research/maya1
url: https://huggingface.co/maya-research/maya1
source: huggingface_trending
type: model_card
date_discovered: 2025-11-21T10:30:00+00:00
date_seen: 2025-11-21
metadata:
  likes: '709'
  downloads: '35150'
  trending_score: '164'
content_preview: Maya1 is a 3B parameter TTS model...
content_length: 8432
```

## Экономия токенов

Артефакты экономят:
- **Время**: не фильтруем уже проверенные элементы
- **Деньги**: меньше запросов к Claude API
- **Повторы**: нет дубликатов в дайджестах

Пример: при 30 элементах в день, через неделю ~210 артефактов, которые автоматически пропускаются.

