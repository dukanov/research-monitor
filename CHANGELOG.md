# Changelog

## 2025-11-21

### Добавлено
- ✅ Парсинг HuggingFace Papers через извлечение JSON из React hydration
- ✅ Keyword-фильтрация статей по темам speech/audio/music (30+ ключевых слов)
- ✅ Фильтрация HF моделей по дате обновления (только за последние 2 недели, сортировка по trending score)
- ✅ Эмодзи для источников в логах (🐙 GitHub, 📄 Papers, 🤖 Models)
- ✅ Улучшенная структура логов с явными этапами (СБОР → ФИЛЬТРАЦИЯ → АГРЕГАЦИЯ → ГЕНЕРАЦИЯ)
- ✅ Debug режим (`--debug`) для сохранения собранных данных и результатов фильтрации
- ✅ Retry логика с exponential backoff для Claude API (обработка 429 rate limits)
- ✅ Батчевание запросов к LLM (2 параллельно по умолчанию)
- ✅ Rate limiting между запросами (1.5s задержка)

### Изменено
- Логи теперь показывают:
  - Какой источник парсится (с эмодзи)
  - Сколько найдено элементов
  - Сколько отфильтровано по ключевым словам/датам
  - Прогресс фильтрации по батчам
  - Релевантность каждого элемента с причинами
- HFPapersSource теперь фильтрует статьи по 30+ ключевым словам
- HFTrendingSource использует дефолтную сортировку (по `trendingScore`) с клиентской фильтрацией по дате
- Добавлен запрос деталей модели для получения `lastModified` (API без sort не возвращает эту информацию)
- Более консервативные дефолты для избежания rate limits (2 параллельных запроса, 1.5s задержка)

### Исправлено
- HuggingFace Papers не парсились (теперь работает через JSON extraction)
- HuggingFace API возвращал 400 из-за неправильного параметра sort
- Ложные срабатывания фильтрации (убраны слишком общие слова типа "synthesis", "ASR")
- Rate limit 429 от Claude API (добавлены retries, batching, увеличены задержки)

## Настройки

Можно управлять через `.env`:
```bash
# Фильтрация HF Papers
# (по умолчанию включена, отключить нельзя через конфиг - нужно менять код)

# Батчинг и rate limits
CONCURRENT_LLM_REQUESTS=5
CLAUDE_REQUEST_DELAY=0.5
CLAUDE_MAX_RETRIES=5
```

